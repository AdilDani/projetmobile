<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- System capabilities -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.FleetTracking">

        <!-- Google Maps API key placeholder -> replace with a real key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.FleetTracking.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Chauffeur space -->
        <activity android:name=".chauffeur.ChauffeurMainActivity" android:theme="@style/Theme.FleetTracking.NoActionBar" />
        <activity android:name=".chauffeur.DeclarerIncidentActivity" android:theme="@style/Theme.FleetTracking.NoActionBar" />
        <activity android:name=".chauffeur.HistoriqueActivity" android:theme="@style/Theme.FleetTracking.NoActionBar" />
        <activity android:name=".chauffeur.TrajetMapActivity" android:theme="@style/Theme.FleetTracking.NoActionBar" />

        <!-- Admin space -->
        <activity android:name=".admin.AdminMainActivity" android:theme="@style/Theme.FleetTracking.NoActionBar" />
        <activity android:name=".admin.VehiculeDetailsActivity" android:theme="@style/Theme.FleetTracking.NoActionBar" />
        <activity android:name=".admin.ChauffeurDetailsActivity" android:theme="@style/Theme.FleetTracking.NoActionBar" />
        <activity android:name=".admin.IncidentsActivity" android:theme="@style/Theme.FleetTracking.NoActionBar" />
        <activity android:name=".admin.NouvelIncidentActivity" android:theme="@style/Theme.FleetTracking.NoActionBar" />
        <activity android:name=".admin.EntretiensActivity" android:theme="@style/Theme.FleetTracking.NoActionBar" />
        <activity android:name=".admin.ProfilActivity" android:theme="@style/Theme.FleetTracking.NoActionBar" />
        <activity android:name=".admin.IncidentDetailsActivity" android:theme="@style/Theme.FleetTracking.NoActionBar" />
        <activity android:name=".admin.EditProfileActivity" android:theme="@style/Theme.FleetTracking.NoActionBar" />
        <activity android:name=".admin.SettingsActivity" android:theme="@style/Theme.FleetTracking.NoActionBar" />
        <activity android:name=".admin.HelpActivity" android:theme="@style/Theme.FleetTracking.NoActionBar" />

    </application>
</manifest>
